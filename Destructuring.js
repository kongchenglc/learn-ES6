//变量的解构赋值

//数组解构赋值
    //解构赋值属于模式匹配，按照对应关系对变量赋值
    let [a, b, c] = [1, 2, 3];
    let [foo, [[bar], baz]] = [1, [[2], 3]];
    let [x, ,y] = [1, 2, 3];
    let [head, ...tail] = [1, 2, 3, 4];

    //解构不成功，变量值为undefined
    let [x, y, ...z] = ['a'];

    //不完全解构
    let [x, y] = [1, 2, 3];